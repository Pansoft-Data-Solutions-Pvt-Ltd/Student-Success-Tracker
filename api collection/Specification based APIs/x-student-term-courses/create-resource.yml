info:
  name: create-resource
  type: http
  seq: 1

http:
  method: POST
  url: "{{tenantUrl}}/api/api-specifications"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      { 
        "id": "00000000-0000-0000-0000-000000000000",
        "resource": "x-student-term-courses",
        "status": "enabled"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
